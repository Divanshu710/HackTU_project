# sample backend module for testing purposes

class Model:
    def check(frame):
        return True
model = Model
def check_if_waste(frame):
    if model.check(frame)==True:
        return True
    else:
        return False